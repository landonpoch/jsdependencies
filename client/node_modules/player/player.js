// Module has dependency on requestHelper, but is completely decoupled
// from requestHelper's file location
module.exports = function Player(requestHelper) {
    var self = this;
    self.requestHelper = requestHelper;
    
    self.executeTest = function() {
        self.requestHelper.printAuthHeader();
    }
    
    return self;
};